% Copyright 2017 Sergei Tikhomirov, MIT License
% https://github.com/s-tikhomirov/solidity-latex-highlighting/

\usepackage{listings, xcolor}

%\definecolor{bgsolidity}{RGB}{209,232,209} % d1e8d1
%\definecolor{bgsolidity}{RGB}{238,255,204} % eeffcc
\definecolor{bgsolidity}{rgb}{.97,.97,.97}
\definecolor{bgdanger}{RGB}{255,209,209} % ffd1d1
% \definecolor{keyword}{RGB}{0,112,32} % 007020
\colorlet{keyword}{blue}
% \definecolor{type}{RGB}{144,32,0} % 902000
\colorlet{type}{teal}
\colorlet{envvar}{violet}
% \definecolor{string}{RGB}{64,112,160} % 4070a0
\colorlet{string}{red}
%\definecolor{env}{RGB}{0,48,128} % 003080

\lstdefinelanguage{Solidity}{
  keywords=[1]{abstract, anonymous, as, assembly, assert, balance, break, call, callcode, case, catch, class, constant, constructor, continue, contract, debugger, default, delegatecall, delete, do, else, emit, event, experimental export, external, fallback, false, finally, for, from, function,% gas,
    if, implements, import, in, indexed, instanceof, interface, internal, is, length, library, log0, log1, log2, log3, log4, memory, modifier, new, payable, pragma, private, protected, public, pure, push, receive, require, return, returns, revert, selfdestruct, send, solidity, staticcall, storage, struct, suicide, super, switch, then, throw, transfer, true, try, typeof, using,% value,
 virtual, view, while, with, addmod, ecrecover, keccak256, mulmod, ripemd160, sha256, sha3}, % generic keywords including crypto operations
	keywordstyle=[1]\color{keyword}\bfseries,
	keywords=[2]{address, bool, byte, bytes, bytes1, bytes2, bytes3, bytes4, bytes5, bytes6, bytes7, bytes8, bytes9, bytes10, bytes11, bytes12, bytes13, bytes14, bytes15, bytes16, bytes17, bytes18, bytes19, bytes20, bytes21, bytes22, bytes23, bytes24, bytes25, bytes26, bytes27, bytes28, bytes29, bytes30, bytes31, bytes32, enum, int, int8, int16, int24, int32, int40, int48, int56, int64, int72, int80, int88, int96, int104, int112, int120, int128, int136, int144, int152, int160, int168, int176, int184, int192, int200, int208, int216, int224, int232, int240, int248, int256, mapping, string, uint, uint8, uint16, uint24, uint32, uint40, uint48, uint56, uint64, uint72, uint80, uint88, uint96, uint104, uint112, uint120, uint128, uint136, uint144, uint152, uint160, uint168, uint176, uint184, uint192, uint200, uint208, uint216, uint224, uint232, uint240, uint248, uint256, var, void, ether, finney, szabo, wei, days, hours, minutes, seconds, weeks, years},	% types; money and time units
	keywordstyle=[2]\color{type}\bfseries,
	keywords=[3]{block, blockhash, coinbase, difficulty, gaslimit, number, timestamp, msg, data, gas, sender, sig, value, now, tx, gasprice, origin,this},	% environment variables
	keywordstyle=[3]\color{envvar}\bfseries,
	identifierstyle=\color{black},
	sensitive=false,
	comment=[l]{//},
	morecomment=[s]{/*}{*/},
	commentstyle=\color{gray}\ttfamily,
	stringstyle=\color{string}\ttfamily,
	morestring=[b]',
	morestring=[b]",
        escapechar=@
}
      
\lstnewenvironment{solidity}[1][]{\lstset{#1}}{}

\lstset{
	language=Solidity,
	backgroundcolor=\color{bgsolidity},
	extendedchars=true,
	basicstyle=\footnotesize\ttfamily,
	showstringspaces=false,
	showspaces=false,
	numbers=left,
	numberstyle=\scriptsize,
	numbersep=9pt,
%        xleftmargin=1.5em,
%        frame=single,
%        framexleftmargin=1.5em,
	tabsize=2,
	breaklines=true,
	showtabs=false,
	captionpos=t,
        keepspaces
      }

\lstMakeShortInline|
      
